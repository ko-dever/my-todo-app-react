{
  "name": "my-todo-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-scripts": "1.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "prebuild": "rm -rf ./build/",
    "postbuild": "echo doomed-world.surge.sh > build/CNAME && surge -p ./build/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
